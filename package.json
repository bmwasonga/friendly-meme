{
  "name": "take-home",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "start": "node server/server.js",
    "server": "nodemon server/server.js",
    "client": "yarn start --prefix client",
    "build": "yarn run server && yarn run client",
    "migrate": "yarn run build && node_modules/.bin/sequelize db:migrate"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "jsonwebtoken": "^8.5.1",
    "passport": "^0.5.2",
    "pg": "^8.7.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.17.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15",
    "sequelize-cli": "^6.4.1"
  }
}